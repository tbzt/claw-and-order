# L'atelier — documents de conception, bancs d'essai, cartes ASCII et
# outillage — vit DEHORS, dans `../claw-and-order-atelier/`. Rien de tout
# ça n'est nécessaire pour faire tourner le jeu, donc rien de tout ça
# n'est publié.
#
# Cette ligne reste comme garde-fou : si un atelier réapparaît un jour
# ici, il ne sera pas publié par accident.
atelier/

# Réglages locaux de l'agent
.claude/

# Python
__pycache__/
*.pyc
